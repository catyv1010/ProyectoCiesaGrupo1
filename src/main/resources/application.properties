# Puerto del servidor
server.port=${PORT:80}

# ===== BASE DE DATOS =====
spring.datasource.url=${DB_URL:jdbc:mysql://ciesa-katalinavalverde-3c42.l.aivencloud.com:19076/defaultdb?ssl-mode=REQUIRED&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USER:avnadmin}
spring.datasource.password=${DB_PASSWORD:AVNS_xkySaRn9R09Q5edFb2S}

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.encoding=UTF-8

# Subida de archivos
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Firebase Storage
firebase.bucket=${FIREBASE_BUCKET:techshop-48e29.firebasestorage.app}
firebase.key=classpath:firebase-key.json
